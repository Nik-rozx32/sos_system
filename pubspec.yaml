name: sos_system
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  google_nav_bar: ^5.0.7
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.5.0
  location: ^8.0.0
  firebase_auth: ^5.5.0
  firebase_messaging: ^15.0.0  # For push notifications
  flutter_local_notifications: ^18.0.1  # Local notifications
  fluttertoast: ^8.2.12
  cloud_firestore: ^5.6.4
  google_fonts: ^6.2.1
  url_launcher: ^6.3.1
  permission_handler: ^11.0.1
  geolocator: ^13.0.2  # For location tracking
  shared_preferences: ^2.5.2
  image_picker: ^1.1.2
  vibration: ^3.1.3
  local_auth: ^2.3.0
  firebase_core: ^3.12.1
  telephony: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/images/engine-warning.png
